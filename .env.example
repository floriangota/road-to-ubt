# Optional. Defaults are in src/config.ts; copy this to .env only to override them.
# For GitHub Pages, the same three names can be set as repo Actions secrets to override.
VITE_SUPABASE_URL=https://yourproject.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-public-key
VITE_COACH_CODE=pick-a-secret-word
